import React, { Component } from 'react';
import './Button.scss';

class Button extends Component {
  render () {
    return (
      <button id="new-quote">
        new quote
      </button>
    );
  }
};

export default Button;
